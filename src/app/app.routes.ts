import { Routes } from '@angular/router';
import { MainComponent } from './subjects/main/main.component';

export const routes: Routes = [

    {
        path:'',
        component:MainComponent
    }
];
